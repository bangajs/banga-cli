const mongoose = require("mongoose");
const Schema = mongoose.Schema;

const <%= $name.toCamelCase() %>Schema = new Schema({
     name: String
});


module.exports = mongoose.model("<%= $name.toCamelCase() %>", <%= $name.toCamelCase() %>Schema)
